prefect==2.20.7
# prefect 2.20.7 exige anyio>=4.4.0,<5.0.0, pero su GatherTaskGroup interno
# (prefect/utilities/asyncutils.py) se rompe contra versiones de anyio 4.x
# más nuevas que las que probó ese release — confirmado corriendo CP-01 de
# punta a punta: las 4 tasks de negocio terminan bien y el flow crashea
# recién al cerrar, con "Can't instantiate abstract class GatherTaskGroup
# without an implementation for abstract method 'create_task'" (pasaba con
# anyio 4.15.1, la última que resuelve pip sin este pin). Fijado a la
# versión mínima que prefect declara soportar.
anyio==4.4.0
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
redis==5.0.8
psycopg[binary]==3.2.3
